# Image-Processing_Lab5
A lab notebook on spatial filtering for image processing, covering both smoothing filters (Box, Gaussian, Median, Bilateral) and sharpening filters (Laplacian, Unsharp Masking) implemented in Python using OpenCV, with comparisons of each filter's effect on the image.
